#spring.application.name=common-lib
server:
  port: 8083

spring:
  application:
    name: common-lib

  datasource:
    url: jdbc:postgresql://localhost:5432/common_lib_db
    username: postgres
    password: Vijay@948
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
